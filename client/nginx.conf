server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This allows React Router to handle the paths without returning a 404
        try_files $uri $uri/ /index.html;
    }
}
